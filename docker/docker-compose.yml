version: '3'
services:
  flask:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./flask-app:/app
    depends_on:
      - opencv
      - yolo

  opencv:
    build: .
    volumes:
      - ./opencv:/app

  yolo:
    build: .
    volumes:
      - ./yolo:/app
